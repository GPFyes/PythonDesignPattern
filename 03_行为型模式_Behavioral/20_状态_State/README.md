# State Pattern（状态模式）

## 定义

允许对象在其内部状态改变时改变它的行为。对象看起来似乎修改了它的类。

## 原理

状态影响行为。不同的状态对应不同的行为，状态 Transition（转换）驱动行为变化。

## 角色

- **Context（上下文）**：维护一个 State 子类的实例，这个实例定义当前状态
- **State（状态）**：定义一个接口以封装与 Context 的一个特定状态相关的行为
- **ConcreteState（具体状态）**：每一个子类实现一个与 Context 的一个状态相关的行为

## 优点

- 消除 if-else 语句：将状态转换逻辑集中到状态类中
- 状态行为集中：每个状态的行为封装在对应的具体状态类中
- 易于扩展：新增状态只需添加新的具体状态类
- 简化 Context：Context 无需了解状态转换细节

## 缺点

- 增加类数量：每个状态都需要一个具体类
- 增加复杂度：状态类和上下文类需要正确设计转换关系
- 状态类可能重复：如果状态行为差异小，可能产生代码重复

## 适用场景

- 对象行为取决于它的状态，且它必须在运行时根据状态改变行为
- 代码中包含大量与对象状态有关的条件语句

## 经典示例

自动售货机（Vending Machine）

## 代码示例

- [入门级 - beginner.py](beginner.py)
- [简单级 - simple.py](simple.py)
- [中级 - intermediate.py](intermediate.py)
- [高级 - advanced.py](advanced.py)
